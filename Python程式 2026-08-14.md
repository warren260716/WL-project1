物件導向（OOP，Object Oriented Programming）：在不同程式可以重複被使用

電腦 （類別）
		物件（Object）包含下面兩個東西：
		共同特徵 → 有CPU，有製造商（屬性 attribute）
		能開機、關機、執行程式（方法 method）
上面全部加起來就叫class（類別）

class（類別）：汽車
attribute（屬性）：廠牌、顔色、型號
method（方法）：開門、關門、發動
[^1]

[^1]: 屬性+方法=物件（Object）

建立物件的動作：實體化（instantiation）

物件都會有編號（id）、型別（type）、值（value）

類別第一個字母通常用大寫（慣例）

建構式（Constructor）：一開始自動執行一些程序
解構式（Destructor）：結束後自動執行一些程序

python程式語言程式區密碼：1234