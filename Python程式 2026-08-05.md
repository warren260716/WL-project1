人類
特徵（男、女、眼睛、手、腳）
行為（吃、喝、睡、走、說話）

程式語言
特徵→屬性（member）
行為→方法（member）

後面.now代表取得現在時間

程式執行是循序的（由上到下），執行的過程中如果遇到函式
不會直接執行，需要另外使用指令呼叫

def 函式名稱(引數1, 引數2,... ... ): 
	程式區塊
	return 傳回值1, 傳回值2,... ...

只有python在return時一次可以回傳多個值

定義函數 ①有沒有回傳值；② 有沒有參數

return → 回傳

被呼叫端 → 虛引數
![[Pasted image 20260805132052.png]]

呼叫端 → 實引數
![[Pasted image 20260805132205.png]]

 傳值呼叫： call by value → 每一個呼叫端、被呼叫端的值各有一個記憶體位置，互不影響
 傳址呼叫： call by reference (address)  → 傳送的是位置不是值

可變物件、串列 → call by reference
不可變物件 → call by value

整個程式從頭到尾都有作用的變數 → 全域變數
程式中間某幾段有作用的變數 → 區域變數

在def下面的是區域變數，在def外面的是全域變數

遞迴：重複動作，一定要找到一個可以停止的節點，不然會有無窮遞迴，程式不會停止

例外處理機制：try開頭、except去抓（輸入錯誤時） → 防呆



