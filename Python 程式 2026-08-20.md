CH09補充
1. getcwd()函式
	import  os
	print(os.getcwd())

2. rmdir一定要是空資料夾，裏面有東西是無法刪除的
3. 在python輸入指令執行後，系統的東西會被執行，要謹慎使用

fName= 'c:/pt/data/stu.txt'，定義字串變數fName，把c:/pt/data/stu.txt當作fName，後續用fName代替c:/pt/data/stu.txt，要使用這個檔案的時候比較方便

fr = open(fName, 'r')，用唯讀的方式打開stu.txt，并指定為fr這個變數，用意是建立程式跟檔案之間的通道

int（n）是只取n整數的部分，小數無條件捨棄

