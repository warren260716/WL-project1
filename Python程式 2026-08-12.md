職訓局提供的copilot business版無法使用MCP（Model Context Protocol）功能

元組（tuple）：跟陣列有點像，也是一個容器（），串列用【】，字典用｛｝，集合用｛｝
字典裏的元素都是鍵值（key，value）

元組裏面的東西可以用不同的元素組成，經過宣告後就不可變更（個數、值），序列是固定的，所以可以用註標值來建立順序

tuple即使只有一個元素，後面也要加","，但如果是一串的元素，最後一個元素後面不用加","，tuple裏面的元素可以重複（list也是）

tuple是認（）及裏面的","，前面的名字隨便寫沒關係

如果是字典（dict），重複的元素會被合併，無序（序列不需要固定）

字典的key是唯一的（不可重複不可改變），只能是數值、字串或元組

字典裏面的值宣告過後是可以改變的

第八章的內容大多要背

下次要講“物件導向”，課本沒有的