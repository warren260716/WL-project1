1. 串列在記憶體中佔用的記憶體位址是連續的
2. 串列的每一個資料都會有一個"註標"(或稱為"索引")
3. 串列中只有一組註標時，稱為一維串列，有兩組註標時，稱為二維串列，以此類推，很少看到有三維以上的東西
4. 串列使用[ ]
5. C、C++、C#是同一系列的語言
6. JAVA是另一種語言
7. Python的特色：串列裏面的元素可以不同型別，也可以不放東西(空集合)在裏面
8. ==讀取串列元素的語法要記==
9. 範例：① A[註標]；② A[0(開始)：3(結束)：2(跳幾個)]
10. Claude code AI：月租型→只能在網頁上使用→費時；API型→跟據你的token(詢問量)來付費
11. OpenAI併購codedex，也可以在IDE下面使用
12. 上面兩種都有一些免費的扣打，多出來的就要付費
13. 如果購買訂閲制的AI，電腦上不要設定API環境變數，不然就會被API制收費
14. 如果vscode按到全螢幕，用F11切換
15. 越晚出來的程式語言越人性化
16. 資料庫維護四個工作：C→ create， R→ receive， U→ update， D→ delete
17. pop：移除指定位置的元素，如果沒有指定，就刪除最後一個；remove：移除指定值的元素，如果沒有指定，就刪除最後一個
18. 有很多種排序法，課本只介紹1種，要看老師的講義
19. Python的表達方式，橫的叫row，直的叫column（excel的欄）
20. Python的寫法：[row][column]