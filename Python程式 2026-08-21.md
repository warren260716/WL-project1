conn = pymysql.connect(host='localhost', port=6033, user='root', password='12345', charset='utf8mb4', database='pythondb')

括號（）
次方  **
正負號、not（反位元）
乘法、除法、* / // %
加法、減法，±
比較> < = !=
and
or

int

== 比較值，內容；is 是不是同一個物件（相同記憶體位址）

classroom[:-3] or classroom[0:-3]