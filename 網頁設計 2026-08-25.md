video，source都不建議使用 → 不好用

type是多餘的
![[Pasted image 20260825101055.png|396]]

網頁設計要背
      三層式架構
      1. table → 這個表格（最外層）
      2. tr → 每一列
      3. th → 表格標題列（最內層最上面那一列）
      4. td → 每一格（最內層）
![[Pasted image 20260825114031.png|462]]

caption：標題的標籤

表格：用來存表單
表單：用來填寫

這個傳送方式叫get，明碼傳遞，會在網址列看到
<form action="表單資料處理目的地" method="傳送方式">

method如果是post， 不會在網址列看到，用來傳密碼等重要訊息，夾在head那邊傳送

![[Pasted image 20260825141644.png]]

type：hidden（隱藏欄位），不顯示在畫面上，但有在執行，很重要
